# Mac環境構築自動化
## 動作環境
- ansible 2.9.2

## 使い方
- ansibleのインストール
```
./InstallAnsible.sh
```
- ansibleの適用
```
ansible-playbook -i hosts exec.yml
```

## あとでコード化するもの
```
brew install --cask chatgpt
brew install --cask docker
```
